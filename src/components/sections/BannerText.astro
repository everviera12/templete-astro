---
import Button from "../ui/Button.astro";

const { title, button=false } = Astro.props;
---

<div
    class="h-100 flex flex-col items-center justify-center gap-10 bg-top bg-cover"
    style={`background-image: url('/background/bg-shops.png');`}
>
    {title && (
        <h2 class="text-center text-7xl font-bold text-white">{title}</h2>
    )}

    {button && (
        <div>
            <Button
                link="https://maps.app.goo.gl/w5egfTo7giSsQhqn7"
                label="Ver nuestra ubicación"
                bgButton="#7209B7"
                bgButtonHover="#724CF9"
                txtButton="white"
                txtHover="white"
            />
        </div>
    )}

</div>
